export declare type ServeConfig = {
    dir?: string;
    port?: number;
    host?: string;
};
export declare type RunConfig = {
    cmd: string;
    watch?: string[];
    reload?: 'all' | 'css' | 'none';
    keepAlive?: boolean;
};
/**
 * The complete config
 */
export declare type StartsConfig = {
    verbose?: boolean;
    serve?: ServeConfig;
    run?: RunConfig[];
    /**
     * autorun initially
     * Default : true
     **/
    autorun?: boolean;
};
