import { ServeConfig } from '../../types';
export declare class Server {
    private config;
    private app;
    private client;
    private server;
    constructor(config: ServeConfig & {
        verbose: boolean;
    });
    triggerReload: () => void;
    triggerReloadCss: () => void;
    private writeLog(logLine);
}
