/// <reference types="ws" />
/// <reference types="node" />
import { Server as WS } from 'ws';
import * as http from 'http';
export declare const clientJsPath: string;
export declare class Livereload {
    options: {
        verbose: boolean;
    };
    constructor(options: {
        verbose: boolean;
    });
    writeLog(logLine: any): void;
    /** Register this as a connect middleware */
    middleware: (req: any, res: any, next: any) => void;
    /**
     * Websocket management
     */
    wss?: WS;
    wsArray: WS['clients'][0][];
    /**
     * Starts a websocket server
     */
    startWS: (server: http.Server) => void;
    triggerReload: (delayMs?: number) => void;
    triggerReloadCss: (delayMs?: number) => void;
}
